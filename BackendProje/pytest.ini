[pytest]
testpaths = tests
markers =
    unit: Unit testler (DB gerektirmez)
    integration: Integration testler (gercek DB)
    api: API endpoint testleri (gercek DB + HTTP)
filterwarnings =
    ignore::DeprecationWarning:passlib
