[pytest]
testpaths = tests
python_files = test_*.py
pythonpath = .
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
markers =
    unit: pure-Python unit tests
    integration: bütün servisi (lifespan dahil) ayağa kaldıran testler
